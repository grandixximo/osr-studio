<Page x:Class="Captura.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      xmlns:models="clr-namespace:Captura.Models;assembly=Captura.Core"
      xmlns:viewModels="clr-namespace:Captura.ViewModels;assembly=Captura.ViewCore"
      xmlns:video="clr-namespace:Captura.Video;assembly=Captura.Core"
      xmlns:screna="clr-namespace:Captura.Video;assembly=Screna"
      xmlns:captura="clr-namespace:Captura"
      xmlns:mui="http://firstfloorsoftware.com/ModernUI"
      DataContext="{Binding MainViewModel, Source={StaticResource ServiceLocator}}">
    <Grid>
        <Grid.Resources>
            <Style TargetType="Path" x:Key="VideoSourceIcon" BasedOn="{StaticResource {x:Type Path}}">
                <Setter Property="Width" Value="20"/>
                <Setter Property="Height" Value="20"/>
                <Setter Property="Margin" Value="5"/>
                <Setter Property="Stretch" Value="Uniform"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Fill" Value="{Binding Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType=ContentControl}}"/>
            </Style>
        </Grid.Resources>
        <StackPanel Margin="5,0,0,0">
            <DockPanel Margin="0,0,0,5">
                <Button Content="{Binding Preview, Source={StaticResource Loc}, Mode=OneWay}"
                        DockPanel.Dock="Right"
                        Command="{Binding ShowPreviewCommand}"
                        Padding="10,0"/>

                <Label Content="{Binding VideoSource, Source={StaticResource Loc}, Mode=OneWay}"
                       FontWeight="Bold"/>
            </DockPanel>
            
            <Border BorderThickness="0.5"
                    BorderBrush="{DynamicResource ModernButtonBorder}"
                    Padding="5,2"
                    Margin="0,2">
                <StackPanel Orientation="Horizontal">
                    <Label Content="{Binding VideoSourcesViewModel.SelectedVideoSourceKind.Name, Source={StaticResource ServiceLocator}}"/>
                    <Label Content="-"
                           Visibility="{Binding VideoSourcesViewModel.SelectedVideoSourceKind.Source.Name, Source={StaticResource ServiceLocator}, Converter={StaticResource NotNullConverter}}"
                           Margin="5,0"/>
                    <Label Content="{Binding VideoSourcesViewModel.SelectedVideoSourceKind.Source.Name, Source={StaticResource ServiceLocator}, TargetNullValue='No Source Selected', FallbackValue='No Source Selected'}"
                           Visibility="{Binding VideoSourcesViewModel.SelectedVideoSourceKind.Source.Name, Source={StaticResource ServiceLocator}, Converter={StaticResource NotNullConverter}}"/>
                </StackPanel>
            </Border>

            <captura:VideoSourceKindList x:Name="VideoSourcesList"/>

            <DockPanel Margin="0,5">
                <Label Content="{Binding AudioFormat, Source={StaticResource Loc}, Mode=OneWay}"
                       Margin="0,0,5,0"/>

                <ComboBox ItemsSource="{Binding VideoSourcesViewModel.NoVideoSourceProvider.Sources, Source={StaticResource ServiceLocator}}"
                          SelectedValue="{Binding VideoSourcesViewModel.NoVideoSourceProvider.SelectedSource, Mode=TwoWay, Source={StaticResource ServiceLocator}}"
                          IsEnabled="{Binding RecordingViewModel.RecorderState, Source={StaticResource ServiceLocator}, Converter={StaticResource NotRecordingConverter}}"/>
                
                <DockPanel.Style>
                    <Style TargetType="DockPanel">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding VideoSourcesViewModel.SelectedVideoSourceKind, Source={StaticResource ServiceLocator}, Converter={StaticResource GetTypeConverter}}" Value="{x:Type video:NoVideoSourceProvider}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DockPanel.Style>
            </DockPanel>

            <StackPanel>
                <Label Content="{Binding VideoEncoder, Source={StaticResource Loc}, Mode=OneWay}"
                       FontWeight="Bold"
                       Margin="0,5"/>

                <ListView Margin="0,5"
                          ItemsSource="{Binding VideoWritersViewModel.VideoWriterProviders, Source={StaticResource ServiceLocator}}"
                          SelectedValue="{Binding VideoWritersViewModel.SelectedVideoWriterKind, Mode=TwoWay, Source={StaticResource ServiceLocator}}"
                          IsEnabled="{Binding RecordingViewModel.RecorderState, Source={StaticResource ServiceLocator}, Converter={StaticResource NotRecordingConverter}}"
                          BorderThickness="0.4"
                          SelectionMode="Single">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="1"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Background="#01000000">
                                <Grid.ToolTip>
                                    <StackPanel MinWidth="200">
                                        <TextBlock Text="{Binding Name, Mode=OneWay}"
                                                   FontWeight="Bold"/>

                                        <TextBlock Text="{Binding Description, Mode=OneWay}"/>
                                    </StackPanel>
                                </Grid.ToolTip>

                                <TextBlock Text="{Binding Name, Mode=OneWay}"
                                           HorizontalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <ComboBox VerticalAlignment="Center"
                          Margin="0,5"
                          ItemsSource="{Binding VideoWritersViewModel.AvailableVideoWriters, Source={StaticResource ServiceLocator}}" 
                          SelectedValue="{Binding VideoWritersViewModel.SelectedVideoWriter, Mode=TwoWay, Source={StaticResource ServiceLocator}}"
                          IsEnabled="{Binding RecordingViewModel.RecorderState, Source={StaticResource ServiceLocator}, Converter={StaticResource NotRecordingConverter}}"
                          Name="VideoWriterComboBox">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding VideoWritersViewModel.AvailableVideoWriters.Count, Source={StaticResource ServiceLocator}}" Value="1">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.Style>
                    <ComboBox.ToolTip>
                        <StackPanel MinWidth="200">
                            <Label Content="{Binding VideoWritersViewModel.SelectedVideoWriter, Mode=OneWay, Source={StaticResource ServiceLocator}}"
                                   FontWeight="Bold"/>

                            <Label Content="{Binding VideoWritersViewModel.SelectedVideoWriter.Extension, Mode=OneWay, Source={StaticResource ServiceLocator}}"
                                   ContentStringFormat="{}File extension: {0}"/>
                            
                            <TextBlock Text="{Binding VideoWritersViewModel.SelectedVideoWriter.Description, Mode=OneWay, Source={StaticResource ServiceLocator}}"
                                       Visibility="{Binding VideoWritersViewModel.SelectedVideoWriter.Description, Source={StaticResource ServiceLocator}, Converter={StaticResource NotNullConverter}}"/>
                        </StackPanel>
                    </ComboBox.ToolTip>
                </ComboBox>

                <!-- Gif Writer settings removed - GifWriterProvider no longer exists in modern codebase -->

                <Grid Margin="0,5"
                      IsEnabled="{Binding RecordingViewModel.RecorderState, Source={StaticResource ServiceLocator}, Converter={StaticResource NotRecordingConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Label Content="{Binding FrameRate, Source={StaticResource Loc}, Mode=OneWay}"
                           ContentStringFormat="{}{0}:"/>
                    <Label Content="{Binding Settings.Video.FrameRate}"
                           Margin="5,0"
                           MinWidth="15"
                           ContentStringFormat="{}{0:N0}"
                           HorizontalContentAlignment="Right"
                           Visibility="{Binding Settings.Video.FpsLimit, Converter={StaticResource BoolToVisibilityConverter}}"
                           Grid.Column="1"/>

                    <Slider Minimum="1"
                            Maximum="30"
                            SelectionStart="8"
                            SelectionEnd="12"
                            IsSelectionRangeEnabled="True"
                            Value="{Binding Settings.Video.FrameRate}"
                            Interval="1"
                            Visibility="{Binding Settings.Video.FpsLimit, Converter={StaticResource BoolToVisibilityConverter}}"
                            Grid.Column="2"/>
                    <xctk:IntegerUpDown Visibility="{Binding Settings.Video.FpsLimit, Converter={StaticResource NegatingConverter}}"
                                        Margin="10,0,0,0"
                                        Minimum="1"
                                        Maximum="120"
                                        Value="{Binding Settings.Video.FrameRate}"
                                        Grid.Column="2"/>

                    <CheckBox IsChecked="{Binding Settings.Video.FpsLimit}"
                              Content="Limit"
                              Margin="5,0,0,0"
                              Grid.Column="3">
                        <CheckBox.ToolTip>
                            <TextBlock MaxWidth="300"
                                       TextWrapping="Wrap">
This Limit option is to prevent system crashes due to using high FPS on full screen or large region sizes.
                            </TextBlock>
                        </CheckBox.ToolTip>
                    </CheckBox>

                    <!-- Gif Writer framerate trigger removed - GifWriterProvider no longer exists -->
                </Grid>

                <DockPanel Margin="0,5"
                           IsEnabled="{Binding RecordingViewModel.RecorderState, Source={StaticResource ServiceLocator}, Converter={StaticResource NotRecordingConverter}}">
                    <Label Content="{Binding Quality, Source={StaticResource Loc}, Mode=OneWay}"
                           ContentStringFormat="{}{0}:"/>
                    <Label Content="{Binding Settings.Video.Quality}"
                           ContentStringFormat="{}{0:N0}%"
                           Margin="5,0"
                           MinWidth="30"
                           HorizontalContentAlignment="Right"/>
                    <Slider Minimum="1" 
                            Maximum="100"
                            SelectionStart="60"
                            SelectionEnd="80"
                            IsSelectionRangeEnabled="True"
                            Value="{Binding Settings.Video.Quality}"/>

                    <DockPanel.Style>
                        <Style TargetType="DockPanel">
                            <Style.Triggers>
                                <!-- GifWriterProvider trigger removed - no longer exists -->
                                <DataTrigger Binding="{Binding VideoWritersViewModel.SelectedVideoWriterKind, Source={StaticResource ServiceLocator}, Converter={StaticResource GetTypeConverter}}" Value="{x:Type video:DiscardWriterProvider}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DockPanel.Style>
                </DockPanel>

                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding VideoSourcesViewModel.SelectedVideoSourceKind, Source={StaticResource ServiceLocator}, Converter={StaticResource GetTypeConverter}}" Value="{x:Type video:NoVideoSourceProvider}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
            </StackPanel>

            <Frame Source="AudioPage.xaml"
                   Margin="0,10,0,0">
                <Frame.Style>
                    <Style TargetType="Frame">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding VideoSourcesViewModel.SelectedVideoSourceKind, Source={StaticResource ServiceLocator}, Converter={StaticResource GetTypeConverter}}" Value="{x:Type video:NoVideoSourceProvider}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Frame.Style>
            </Frame>

            <DockPanel>
                <DockPanel.Style>
                    <Style TargetType="DockPanel">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding VideoSourcesViewModel.SelectedVideoSourceKind, Source={StaticResource ServiceLocator}, Converter={StaticResource GetTypeConverter}}" Value="{x:Type video:NoVideoSourceProvider}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DockPanel.Style>

                <GridSplitter IsEnabled="False"
                              DockPanel.Dock="Top"
                              Height="1"
                              Margin="0,10"/>

                <StackPanel>
                    <Label Content="{Binding Audio, Source={StaticResource Loc}, Mode=OneWay}"
                           FontWeight="Bold"
                           Margin="0,5"/>

                    <xctk:DropDownButton ToolTip="{Binding Audio, Source={StaticResource Loc}, Mode=OneWay}"
                                         Padding="5"
                                         HorizontalAlignment="Left"
                                         Name="AudioDropdown">
                        <Path Data="{Binding Icons.Mic, Source={StaticResource ServiceLocator}}">
                            <Path.Style>
                                <Style TargetType="Path" BasedOn="{StaticResource VideoSourceIcon}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Settings.Audio.RecordMicrophone}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource Accent}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Settings.Audio.RecordSpeaker}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource Accent}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Path.Style>
                        </Path>

                        <xctk:DropDownButton.DropDownContent>
                            <Frame Source="AudioPage.xaml"
                                   Margin="10"/>
                        </xctk:DropDownButton.DropDownContent>
                    </xctk:DropDownButton>
                </StackPanel>

                <Frame Source="WebcamPage.xaml"
                       Margin="15,0,0,0"/>
            </DockPanel>

            <StackPanel>
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding VideoSourcesViewModel.SelectedVideoSourceKind, Source={StaticResource ServiceLocator}, Converter={StaticResource GetTypeConverter}}" Value="{x:Type video:NoVideoSourceProvider}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>

                <GridSplitter IsEnabled="False"
                              Height="1"
                              Margin="0,10"/>

                <Label Content="{Binding ScreenShot, Source={StaticResource Loc}, Mode=OneWay}"
                       FontWeight="Bold"/>

                <StackPanel Margin="0,5"
                            DataContext="{Binding ScreenShotViewModel, Source={StaticResource ServiceLocator}}"
                            Orientation="Horizontal">
                    <captura:ModernToggleButton ToolTip="{Binding Disk, Source={StaticResource Loc}, Mode=OneWay}"
                                                IconData="{Binding Icons.NewFile, Source={StaticResource ServiceLocator}}"
                                                IsChecked="{Binding DiskWriter.Active}"/>

                    <captura:ModernToggleButton ToolTip="{Binding Clipboard, Source={StaticResource Loc}, Mode=OneWay}"
                                                IconData="{Binding Icons.Clipboard, Source={StaticResource ServiceLocator}}"
                                                IsChecked="{Binding ClipboardWriter.Active}"/>

                    <!-- Imgur upload button removed - feature no longer supported -->

                    <captura:ModernToggleButton ToolTip="{Binding ImageEditor, Source={StaticResource Loc}, Mode=OneWay}"
                                                IconData="{Binding Icons.Pencil, Source={StaticResource ServiceLocator}}"
                                                IsChecked="{Binding EditorWriter.Active, Source={StaticResource ServiceLocator}}"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
